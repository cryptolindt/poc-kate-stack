apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets
  namespace: external-secrets
  annotations:
    # ESO_CLIENT_ID is replaced by the aks-bootstrap workflow at apply time
    azure.workload.identity/client-id: ESO_CLIENT_ID
  labels:
    azure.workload.identity/use: "true"
